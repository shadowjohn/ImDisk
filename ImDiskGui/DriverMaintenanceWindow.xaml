<Window x:Class="ImDiskGui.DriverMaintenanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ImDiskGui"
        Title="{Binding Source={x:Static local:LanguageManager.Instance}, Path=[DriverMaintenance]}"
        SizeToContent="WidthAndHeight"
        Width="520"
        Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Source={x:Static local:LanguageManager.Instance}, Path=[DriverMaintenance]}"
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Text="{Binding Source={x:Static local:LanguageManager.Instance}, Path=[DriverRequiredMessage]}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="BtnInstall"
                    Content="{Binding Source={x:Static local:LanguageManager.Instance}, Path=[DriverInstallTitle]}"
                    Width="132" Height="30" Margin="0,0,8,0"
                    Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"
                    Click="BtnInstall_Click"/>
            <Button x:Name="BtnUninstall"
                    Content="{Binding Source={x:Static local:LanguageManager.Instance}, Path=[DriverUninstall]}"
                    Width="116" Height="30" Margin="0,0,8,0"
                    Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"
                    Click="BtnUninstall_Click"/>
            <Button Content="{Binding Source={x:Static local:LanguageManager.Instance}, Path=[DlgClose]}"
                    Width="88" Height="30"
                    Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
